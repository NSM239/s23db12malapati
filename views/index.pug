extends layout

block content
  h1= title
  p welcome to Navya household cleaning  

  button(type="button",class="btn btn-success", onclick="doReduce()") Press Me
  div(id="reduce") Reduce the value.
  script. 
    var count = 20;
    var change = -1;
    console.log("Navya")
    function doReduce() {
      count += change 
      change -= 1
      document.getElementById("reduce").innerHTML = "Count " + count + " wtih change " + change 

      }
    
